{
  "plan": {
    "businessContext": "Every referral is contacted within 4 business hours of intake, and the outcome is logged.",
    "workflows": [],
    "assumptions": [
      {
        "type": "needs_confirmation",
        "text": "Assumed a 3-attempt cap before expiring to no_answer -- confirm this matches Empire's real call policy."
      }
    ],
    "sheetsColumns": [],
    "testChecklist": []
  },
  "traceability": [],
  "escalation": {
    "reason": "This ProcessContract fails deterministic validation and cannot be compiled until fixed. Run `kairos contract validate` for the full list.",
    "questions": [
      "[Rule 1] Transition \"t-received-to-attempted\" references unknown fromState \"does_not_exist\" (t-received-to-attempted)",
      "[Rule 3] State \"contact_attempted\" is unreachable -- no path from any StartCondition.initialState reaches it (contact_attempted)",
      "[Rule 3] State \"contacted\" is unreachable -- no path from any StartCondition.initialState reaches it (contacted)",
      "[Rule 3] State \"scheduled\" is unreachable -- no path from any StartCondition.initialState reaches it (scheduled)",
      "[Rule 3] State \"declined\" is unreachable -- no path from any StartCondition.initialState reaches it (declined)",
      "[Rule 3] State \"no_answer\" is unreachable -- no path from any StartCondition.initialState reaches it (no_answer)"
    ],
    "source": "validation_errors"
  }
}
